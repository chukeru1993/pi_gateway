# pi HTTP Gateway — 开发计划

> 基于 `1.md` 方案文档，拆解为可执行的阶段化开发任务。

---

## 项目结构

```
pi-gateway/
├── src/
│   ├── index.ts                  # 入口：创建 Fastify 实例，注册插件/路由
│   ├── pi-process.ts             # PiProcess：RPC 子进程封装
│   ├── pi-process-pool.ts        # PiProcessPool：会话池管理
│   ├── metrics.ts                # GatewayMetrics / SessionSnapshot 类型 + 收集
│   ├── sse-writer.ts             # createSSEWriter 工具
│   ├── auth.ts                   # verifyToken + JWT 认证中间件
│   ├── security.ts               # validateCwd / validateBash / rateLimit
│   ├── routes/
│   │   ├── sessions.ts           # POST /sessions, DELETE /sessions/:id
│   │   ├── state.ts              # GET /sessions/:id/state, GET /sessions/:id/stats
│   │   ├── chat.ts               # POST /sessions/:id/chat (SSE 核心)
│   │   ├── ui-response.ts        # POST /sessions/:id/ui-response, GET /pending-ui-requests
│   │   ├── model-config.ts       # 模型/思考级别/配置相关端点
│   │   ├── session-ops.ts        # messages/compact/bash/abort-bash/commands/name/last-response/export-html
│   │   ├── lifecycle.ts          # new-session/switch/fork/clone/fork-messages
│   │   └── admin.ts              # /admin 面板 + /admin/api/* JSON 端点
│   └── dashboard-html.ts         # getDashboardHTML() 内嵌页面
├── test/
│   ├── pi-process.test.ts
│   ├── pi-process-pool.test.ts
│   ├── routes/
│   │   ├── sessions.test.ts
│   │   ├── chat.test.ts
│   │   ├── ui-response.test.ts
│   │   └── integration.test.ts
│   └── dashboard.test.ts
├── Dockerfile
├── docker-compose.yml
├── tsconfig.json
└── package.json
```

---

## 阶段零：项目初始化

**目标**：创建 TypeScript 项目骨架，安装依赖，确保编译通过。

**依赖**：无 | **预估**：0.5h

### 任务 0.1 初始化项目

| 属性 | 内容 |
|------|------|
| 输入 | Node.js >= 22 |
| 输出 | `package.json`、`tsconfig.json`、`src/index.ts`（空 Fastify 启动） |
| 验收标准 | `npm run build` 成功，`node dist/index.js` 启动后 `curl http://localhost:3000/health` 返回 200 |

**执行步骤**：

1. `mkdir pi-gateway && cd pi-gateway && npm init -y`
2. 安装依赖：`fastify`、`@fastify/rate-limit`（dev：`typescript`、`@types/node`、`tsx`）
3. 创建 `tsconfig.json`（target ES2022, module NodeNext, outDir dist, strict）
4. 创建 `src/index.ts`：
   ```typescript
   import Fastify from "fastify";
   const app = Fastify({ logger: true });
   app.get("/health", async () => ({ status: "ok" }));
   app.listen({ port: 3000, host: "0.0.0.0" });
   ```
5. 添加 scripts：`"build": "tsc"`, `"dev": "tsx watch src/index.ts"`, `"start": "node dist/index.js"`

**验证**：

- [ ] `npm run build` 无错误
- [ ] `node dist/index.js` 启动，`curl localhost:3000/health` → `{"status":"ok"}`

---

## 阶段零.5：RPC 接口契约验证

**目标**：在编写代码之前，通过实际运行 pi RPC 模式，验证所有 RPC 命令的请求/响应格式，确保设计文档与真实 pi 行为一致。

**依赖**：阶段零 | **预估**：1h

### 任务 0.5.1 启动 pi --mode rpc 并记录协议

| 属性 | 内容 |
|------|------|
| 输入 | `pi --mode rpc` 子进程 |
| 输出 | 验证报告：各命令的 request/response 格式、SSE 事件类型与实际字段名 |
| 验收标准 | 所有 26 个端点的对应 RPC 命令均在真实 pi 进程上验证通过 |

**执行步骤**：

1. 启动 `pi --mode rpc`，通过 stdin 发送 `{"type":"get_state","id":"test_1"}` 确认响应格式
2. 依次验证以下命令组，记录准确的字段名和数据类型：
   - 基础：`get_state`, `get_session_stats`, `get_messages`, `get_available_models`
   - 对话：`prompt` (含 `message`, `images`, `streamingBehavior` 字段)
   - 控制：`steer`, `follow_up`, `abort`
   - 模型：`set_model`, `cycle_model`, `set_thinking_level`, `cycle_thinking_level`
   - 配置：`set_steering_mode`, `set_follow_up_mode`, `set_auto_compaction`, `set_auto_retry`
   - 会话：`compact`, `bash`, `abort_bash`, `get_commands`, `set_session_name`, `get_last_assistant_text`, `export_html`
   - 生命周期：`new_session`, `switch_session`, `fork`, `clone`, `get_fork_messages`
3. 发送一个 prompt（如 "echo hello"），记录所有 SSE 事件类型和字段结构：
   - 验证 `agent_start`, `turn_start`, `message_start`, `message_update` (各子类型), `message_end`, `turn_end`, `agent_end` 的顺序和结构
   - 验证 `tool_execution_start/update/end` 的事件结构
   - 验证 `extension_ui_request` 中 `confirm`/`notify`/`setStatus`/`setWidget`/`setTitle`/`set_editor_text` 的实际字段名
4. 对比 `1.md` 中的接口定义与真实行为，标记差异

**验证**：
- [ ] 所有 RPC 命令均可成功调用，返回 `{"type":"response","success":true,...}`
- [ ] SSE 事件字段名与文档一致（或已更新文档）
- [ ] `extension_ui_request` 各 method 的字段名已验证
- [ ] 未知差异已记录到 `CONTRACT-ISSUES.md`，供后续阶段处理

---

## 阶段一：PiProcess — RPC 子进程封装

**目标**：实现对 pi RPC 子进程的 JSONL 通信、命令/响应关联、事件订阅、背压写入、生命周期管理、内存监控（子进程级别）、无响应检测。

**依赖**：阶段零.5 | **预估**：4h

### 任务 1.1 JSONL 通信层

| 属性 | 内容 |
|------|------|
| 输入 | `ChildProcess` (spawn 自 `"pi" --mode rpc`) |
| 输出 | `attachReader()` 可正确解析 JSONL 行；`write()` 可背压安全地写入 JSONL |
| 验收标准 | 向 stdin 写 `{"type":"get_state"}\n`，stdout 返回 `{"type":"response","command":"get_state","success":true}` 时 promise 被 resolve |

**实现要点**：

- `attachReader()`：按 `\n` 分割 buffer，处理尾部 `\r`，逐行 `JSON.parse` 后调用 `handleLine`
- `handleLine(msg)`：
  - `msg.type === "response" && msg.id && pendingResponses.has(msg.id)` → resolve/reject 对应 promise
  - 否则 → 广播给所有 `eventListeners`
- `write(obj)`：背压安全机制见 1.3

### 任务 1.2 命令/响应关联

| 属性 | 内容 |
|------|------|
| 输入 | RPC 命令对象（不含 `id`） |
| 输出 | `sendCommand(cmd, timeout)` 返回 Promise，resolution 为 RPC 响应 |
| 验收标准 | 连续发 3 个 `get_state`，3 个 promise 按各自 id 正确 resolve |

**实现要点**：

- `requestId` 自增计数器，id 格式 `req_1`
- 30 秒默认超时，超时后 reject 并从 `pendingResponses` 中清理
- `sendCommand` 在命令对象上附加 `id` 后调用 `write()`

### 任务 1.3 stdin 背压处理

| 属性 | 内容 |
|------|------|
| 输入 | 高频写入（如连续多个 prompt+steer） |
| 输出 | `write()` 在 `stdin.write()` 返回 `false` 时进入 draining 模式，队列积压消息，`drain` 事件后批量写出 |
| 验收标准 | 模拟快速发送 100 条小命令，无 `EPIPE` 或数据丢失 |

**实现要点**：

- `writeQueue: string[]` + `draining: boolean`
- `stdin.write(line)` 返回 `false` 时设 `draining = true`，注册一次性 `drain` handler
- `drain` handler 写出 `writeQueue` 中所有积压行，`draining = false`

### 任务 1.4 事件订阅

| 属性 | 内容 |
|------|------|
| 输入 | `AgentEvent`（从 pi stdout 收到） |
| 输出 | `onEvent(listener)` 返回取消函数，所有非 response 消息 + extension_ui_request 广播给所有 listener |
| 验收标准 | RPC 子进程在处理 prompt 时发出的 `agent_start` / `message_start` / `message_update` / `agent_end` 事件被所有已注册 listener 收到 |

### 任务 1.5 生命周期管理

| 属性 | 内容 |
|------|------|
| 输入 | `shutdown()` 调用或 `exit` 事件 |
| 输出 | 正确清理 timer、关闭子进程、通知 listener |
| 验收标准 | 调用 `shutdown()` 后子进程在 1s 内退出（先 SIGTERM 后 SIGKILL）；`intentionalShutdown` 为 true 时不通知 error |

**实现要点**：

- `shutdown()`：清理 idleTimer + memoryTimer + hangTimer → `proc.kill("SIGTERM")` → 1s timeout 后 `SIGKILL`
- 进程 `exit` 事件：`intentionalShutdown === false` 时向所有 listener 广播 error 事件
- 进程 `error` 事件：同上

### 任务 1.6 子进程内存监控

| 属性 | 内容 |
|------|------|
| 输入 | 子进程 pid |
| 输出 | `getChildProcessMemory()` 返回子进程 RSS（字节）；`startMemoryMonitor(maxBytes)` 定期检查 |
| 验收标准 | Linux 上读 `/proc/[pid]/statm`，macOS 上执行 `ps -o rss= -p [pid]`，均返回有效 RSS |

**实现要点**：

- `getChildProcessMemory()`：优先读 `/proc/[pid]/statm`（第二列页数 × 4096），fallback 到 `ps -o rss=`（KB × 1024）
- `startMemoryMonitor(maxBytes)`：每 10s 调用 `getChildProcessMemory()`，超阈值时向 listener 发 error 事件并 `pool.destroy()`
- **注意**：不可使用 `process.memoryUsage()`，那是 Gateway 自身的内存

### 任务 1.7 无响应检测（Hang Detection）

| 属性 | 内容 |
|------|------|
| 输入 | 子进程 stdout 的最近活跃时间 |
| 输出 | 超过 5 分钟无 stdout 输出时判定 hang，SIGKILL 强杀并通知 listener |
| 验收标准 | 模拟 pi 进程 hang 住 → 5 分钟后 listener 收到 error 事件 + 子进程退出 |

**实现要点**：

- `hangTimer: NodeJS.Timeout`，`HANG_TIMEOUT = 5 * 60_000`
- `attachReader()` 每次处理行时调用 `resetHangTimer()`
- hang timer 到期 → `proc.kill("SIGKILL")` + 广播 error 事件

**阶段一验证清单**：

- [ ] 启动 pi 子进程，发送 `get_state`，收到有效 `RpcSessionState`
- [ ] 连续 10 条命令全部正确 resolve，无超时、无串号
- [ ] 100 条急速命令无 EPIPE
- [ ] 手动 `shutdown()`：子进程 1s 内退出，timer 已清理
- [ ] 杀 pi 进程（模拟崩溃）：listener 收到 error 事件
- [ ] `getChildProcessMemory()` 在 Linux 和 macOS 上返回有效 RSS 值（非 0）
- [ ] 将内存阈值临时设为很低的值 → 子进程被驱逐 + listener 收到 error
- [ ] 子进程 5 分钟无 stdout 输出 → hang 检测触发 → SIGKILL + listener 收到 error

---

## 阶段二：PiProcessPool — 会话池管理

**目标**：管理多个 PiProcess 实例的生命周期，包含并发控制、空闲回收、内存监控。

**依赖**：阶段一 | **预估**：2h

### 任务 2.1 进程创建与销毁

| 属性 | 内容 |
|------|------|
| 输入 | `sessionId + CreateOptions` |
| 输出 | 新 PiProcess 加入池；`destroy(sessionId)` 从池中移除并 shutdown |
| 验收标准 | 创建 3 个会话 → `pool.size === 3`；销毁 1 个 → `pool.size === 2`；子进程已退出 |

**实现要点**：

- `PiProcessPool.processes: Map<string, PiProcess>`
- `create()`：检查 `maxTotal` 限制 → spawn → 存入 map
- `destroy()`：从 map 删除 → `pi.shutdown()`
- 暴露 `all(): Iterable<[string, PiProcess]>` 用于遍历
- 暴露 `size: number` getter

### 任务 2.2 空闲超时回收

| 属性 | 内容 |
|------|------|
| 输入 | 会话创建时启动 30 分钟空闲 timer |
| 输出 | 30 分钟无 `resetIdleTimer()` 调用 → 自动 `destroy` |
| 验收标准 | 创建会话，等待 30+ 分钟（或测试时改为 5s），会话被自动销毁 |

**实现要点**：

- `pi.startIdleTimer(30 * 60_000)` 在 pool.create 后调用
- `pi.resetIdleTimer()` 在每次 chat SSE 连接事件时调用
- timer 到期 → `pool.destroy(sessionId)`
- `shutdown()` 时清除 timer

### 任务 2.3 系统内存压力驱逐

| 属性 | 内容 |
|------|------|
| 输入 | 定时检查系统总内存使用率 |
| 输出 | 超过 80% → 驱逐最旧的空闲会话 |
| 验收标准 | 将 maxTotalRatio 临时设为 0.01，确认空闲会话被驱逐 |

**实现要点**：

- **注意**：单进程内存监控已在 阶段一/任务 1.6 实现（`PiProcess.getChildProcessMemory()`），此处只做系统级补充
- `MemoryGuard`：每 30s 检查 `usedMem / totalMem > 0.8`，触发时驱逐最久未活跃的 `idle` 会话
- 驱逐时写入 `recentEvents`：类型 `ejected`，detail 标明原因

**阶段二验证清单**：

- [ ] 创建 50+1 个会话 → 第 51 个抛错 "Maximum concurrent sessions reached"
- [ ] 空闲 5 秒（测试配置）→ 会话自动销毁
- [ ] 模拟系统内存超 80% → 最旧空闲会话被驱逐，recentEvent 记录 ejected

---

## 阶段三：Metrics 层

**目标**：实现 GatewayMetrics / SessionSnapshot / RecentEvent 数据收集，供 admin panel 消费。

**依赖**：阶段二 | **预估**：1h

### 任务 3.1 指标类型与收集

| 属性 | 内容 |
|------|------|
| 输入 | `PiProcessPool` 和 `PiProcess` 上的关键操作点 |
| 输出 | `metrics.ts` 导出的 `collectEvent()`, `getSnapshot()`, `getMetrics()` 函数 |
| 验收标准 | `GET /admin/api/metrics` 返回值包含正确的 `activeSessions`, `totalPrompts`, `recentEvents` |

**实现要点**：

- `GatewayMetrics` 接口（`uptime`, `totalSessionsCreated/Destroyed`, `activeSessions`, `maxSessions`, `totalPrompts`, `totalErrors`, `gatewayMemoryMB`, `systemMemoryPercent`, `events: RecentEvent[]`）
- `RecentEvent` 接口（`timestamp`, `sessionId`, `type`, `detail`）
- `SessionSnapshot` 接口（`sessionId`, `status`, `model`, `provider`, `cwd`, `messageCount`, `pendingMessageCount`, `steeringQueue`, `followUpQueue`, `thinkingLevel`, `autoCompactionEnabled`, `autoRetryEnabled`, `createdAt`, `lastActivityAt`, `memoryMB`, `pendingUiQuestions`）
- `collectEvent(pool, type, sessionId, detail)` 追加到环形缓冲区（最大 50 条）
- `getSessionSnapshot(pi)` 从 PiProcess 提取快照
- `getMetrics(pool)` 聚合全局指标

**埋点位置**：

| 操作 | 事件类型 |
|------|---------|
| pool.create() | `created` |
| pool.destroy() | `destroyed` |
| chat handler 发送 prompt | `prompt` |
| 子进程 crash / 驱逐 | `error` / `ejected` |
| compaction_end 事件 | `compaction` |

### 任务 3.2 PiProcess 补充状态字段

| 属性 | 内容 |
|------|------|
| 输入 | RPC 事件流中提取的会话状态 |
| 输出 | PiProcess 上增加以下字段的实时更新逻辑 |
| 验收标准 | `getSessionSnapshot(pi)` 返回的字段值与实际 pi 子进程状态一致 |

**需要实现的字段**（见 `1.md` PiProcess 类定义）：

| 字段 | 来源 | 更新时机 |
|------|------|---------|
| `cwd` | 构造函数 options | 会话创建时 |
| `currentModel` | `get_state` response + `cycle_model` response | response 处理时 |
| `currentProvider` | `get_state` response | response 处理时 |
| `thinkingLevel` | `get_state` response + `cycle_thinking_level` response | response 处理时 |
| `isStreaming` | `agent_start` → true, `agent_end` → false | handleLine 事件 switch |
| `isCompacting` | `compaction_start` → true, `compaction_end` → false | handleLine 事件 switch |
| `messageCount` | `get_state` response | response 处理时 |
| `pendingMessageCount` | `get_state` response | response 处理时 |
| `steeringQueue` | `queue_update` 事件 | handleLine 事件 switch |
| `followUpQueue` | `queue_update` 事件 | handleLine 事件 switch |
| `autoCompactionEnabled` | `get_state` response | response 处理时 |
| `autoRetryEnabled` | `get_state` response | response 处理时 |
| `createdAt` | 构造函数 | `Date.now()` |
| `lastActivityAt` | handleLine 入口 | 每次收到 stdout 行 |
| `activeSSEs` | chat handler 注册/注销 | SSE 连接建立/关闭时 |

**实现要点**：

- 在 `handleLine` 中根据 `msg.type` 做 switch 更新以上字段（代码已在 `1.md` PiProcess 类中给出）
- `getSessionSnapshot(pi)` 直接读取这些字段，无需额外 RPC 调用

**阶段三验证清单**：

- [ ] 创建/销毁会话后 `recentEvents` 包含正确条目
- [ ] pi 子进程 streaming 时 `getSessionSnapshot` 返回 `status: "streaming"`
- [ ] 超过 50 条事件时环形缓冲区正确覆盖最旧条目

---

## 阶段四：SSE Writer 工具

**目标**：封装 SSE 帧输出函数，处理背压和连接关闭。

**依赖**：阶段零 | **预估**：0.5h

### 任务 4.1 createSSEWriter

| 属性 | 内容 |
|------|------|
| 输入 | `ServerResponse` (Node http) |
| 输出 | `sse.write(event, data)` 和 `sse.end()` |
| 验收标准 | 写 3 条 SSE 事件后 `sse.end()`，客户端收到完整的 `text/event-stream` 响应 |

**实现要点**：

```typescript
export function createSSEWriter(res: ServerResponse) {
  return {
    write(event: string, data: unknown) {
      res.write(`event: ${event}\ndata: ${JSON.stringify(data)}\n\n`);
    },
    end() {
      res.end();
    },
  };
}
```

**阶段四验证清单**：

- [ ] SSE 心跳定时器写入 `: heartbeat\n\n` 不触发客户端事件

---

## 阶段五：HTTP 路由 — 会话管理

**目标**：实现会话 CRUD 端点。

**依赖**：阶段二 + 阶段四 | **预估**：1.5h

### 任务 5.1 POST /sessions

| 属性 | 内容 |
|------|------|
| 输入 | `{ provider?, model?, cwd?, noSession?, sessionDir?, thinkingLevel? }` |
| 输出 | `201 { sessionId, status: "ready" }` |
| 验收标准 | curl POST → 201，pi 子进程已启动；重复创建不同 sessionId 的会话成功 |

**实现要点**：

- 生成 `sessionId`（`sess_` + nanoid 或 crypto.randomUUID 前 12 位）
- 调用 `pool.create(sessionId, options)`
- 若创建时抛错（如达到上限），返回 503
- 记录 `recentEvents`：`created`

### 任务 5.2 DELETE /sessions/:id

| 属性 | 内容 |
|------|------|
| 输入 | URL 参数 `:id` |
| 输出 | `200 { ok: true }` 或 `404 { error }` |
| 验收标准 | 删除存在的会话 → 200，子进程退出；删除不存在的 → 404 |

### 任务 5.3 GET /sessions/:id/state

| 属性 | 内容 |
|------|------|
| 输入 | URL 参数 `:id` |
| 输出 | `200 { model, thinkingLevel, isStreaming, isCompacting, ... }` |
| 验收标准 | 返回值与 `get_state` RPC 命令返回值一致 |

**实现要点**：

- `await pi.sendCommand({ type: "get_state" })` → 从 `data` 提取 `RpcSessionState`

### 任务 5.4 GET /sessions/:id/stats

| 属性 | 内容 |
|------|------|
| 输入 | URL 参数 `:id` |
| 输出 | `200 { sessionFile, userMessages, totalMessages, tokens, cost, contextUsage }` |
| 验收标准 | 包含 token 用量字段 |

**实现要点**：

- `await pi.sendCommand({ type: "get_session_stats" })` → 提取 data

**阶段五验证清单**：

- [ ] POST /sessions → 201, pi 进程启动
- [ ] GET /sessions/:id/state → 包含 model/thinkingLevel 等所有字段
- [ ] DELETE /sessions/:id → 200 + 进程退出
- [ ] 删除后的会话 ID 再查 state → 404

---

## 阶段六：HTTP 路由 — 对话核心（SSE 流）

**目标**：实现 `POST /sessions/:id/chat`，将 pi 事件流转发为 SSE 流。

**依赖**：阶段二 + 阶段三 + 阶段四 | **预估**：3h

### 任务 6.1 POST /sessions/:id/chat — 基本流

| 属性 | 内容 |
|------|------|
| 输入 | `{ message, images?, streamingBehavior? }` |
| 输出 | SSE 事件流（详见下方事件映射表） |
| 验收标准 | curl POST → 收到 `agent_start` → `message_start` → `message_update` → `text_delta`... → `message_end` → `agent_end` |

**实现步骤**：

1. 检查 pi 是否存在 → 404
2. 检查是否已有活跃 SSE 连接 → 409
3. 设置 response headers（Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no）
4. 创建 `sse = createSSEWriter(reply.raw)` 并 `pi.activeSSEs.add(sse)`
5. 注册 `pi.onEvent` listener（见任务 6.2）
5. 注册 `req.raw.on("close")` → unsubscribe + pi.activeSSEs.delete(sse) + resetIdleTimer
6. 发送 `sendCommand({ type: "prompt", message, images, streamingBehavior })`

### 任务 6.2 SSE 事件路由

| 属性 | 内容 |
|------|------|
| 输入 | pi 子进程的 stdout 事件 |
| 输出 | `sse.write(eventType, payload)` |

**事件映射表**：

| RPC 事件/消息 | SSE event 名称 | data 内容 |
|--------------|---------------|----------|
| `extension_ui_request` (select/confirm/input/editor) | `user_question` | `{ id, method, title, message?, options?, placeholder?, prefill?, timeout? }`，同时调用 `pi.registerPendingQuestion()` |
| `extension_ui_request` (notify) | `notification` | `{ id, method, message, notifyType }` |
| `extension_ui_request` (setStatus) | `status_update` | `{ id, statusKey, statusText }` |
| `extension_ui_request` (setWidget) | `widget_update` | `{ id, widgetKey, widgetLines?, widgetPlacement? }` |
| `extension_ui_request` (setTitle) | `title_update` | `{ id, title }` |
| `extension_ui_request` (set_editor_text) | `editor_text_update` | `{ id, text }` |
| `agent_start` / `agent_end` | `agent_start` / `agent_end` | 原始 event |
| `turn_start` / `turn_end` | `turn_start` / `turn_end` | 原始 event |
| `message_start` / `message_end` | `message_start` / `message_end` | `{ message }` |
| `message_update` | `message_update` | 原始 event（含 `assistantMessageEvent` 嵌套结构） |
| `tool_execution_start` / `update` / `end` | `tool_execution_start` / `tool_execution_update` / `tool_execution_end` | 原始 event |
| `compaction_start` / `compaction_end` | `compaction_start` / `compaction_end` | 原始 event |
| `auto_retry_start` / `auto_retry_end` | `auto_retry_start` / `auto_retry_end` | 原始 event |
| `queue_update` | `queue_update` | 原始 event |
| `extension_error` | `extension_error` | 原始 event |
| (PiProcess 内部 error) | `error` | `{ message }` |

**验收标准**：

- pi 扩展调用 `ctx.ui.confirm("Allow?", "rm -rf /tmp")` → 前端收到 `user_question` SSE 事件且 `method: "confirm"`
- pi 扩展调用 `ctx.ui.notify("Blocked", "warning")` → 前端收到 `notification` SSE 事件
- pi 扩展调用 `ctx.ui.setStatus("ext", "Running")` → 前端收到 `status_update` SSE 事件
- 标准 `message_update` (text_delta) → 前端收到完整的 `message_update` SSE 事件且保留 `assistantMessageEvent` 嵌套

### 任务 6.3 SSE 连接排他性与清理

| 属性 | 内容 |
|------|------|
| 输入 | 同一会话的第二个 chat 请求 |
| 输出 | 409 Conflict |
| 验收标准 | curl 第一个 chat 连接保持 + curl 第二个 chat → 409 |

**实现要点**：

- PiProcess 维护 `activeSSEs: Set` 或标记 `hasActiveSSE: boolean`
- 连接 close 时从集合中移除

### 任务 6.4 POST /sessions/:id/steer, /follow-up, /abort

| 属性 | 内容 |
|------|------|
| 输入 | `/steer`: `{ message, images? }`；`/follow-up`: `{ message, images? }`；`/abort`: 无 body |
| 输出 | `200 { ok: true }` |
| 验收标准 | steer 命令在 pi 子进程正在 streaming 时被正确投递到 steering queue |

**实现要点**：

- steerm: `pi.sendCommand({ type: "steer", message, images })`
- follow-up: `pi.sendCommand({ type: "follow_up", message, images })`
- abort: `pi.sendCommand({ type: "abort" })`

**阶段六验证清单**：

- [ ] 发送 prompt → SSE 流中按序收到 agent_start → message_start → text_delta → text_end → done → message_end → agent_end
- [ ] `message_update` 事件中 `assistantMessageEvent.type === "thinking_delta"` 时 delta 字段正确
- [ ] `extension_ui_request` confirm → 前端收到 `user_question` SSE 事件，method: "confirm"
- [ ] `extension_ui_request` notify → 前端收到 `notification` SSE 事件，无 user_question
- [ ] SSE 连接断开后 pi 的事件不再写入 response
- [ ] 同一会话第二个 chat → 409
- [ ] steer 命令在 streaming 期间发送成功

---

## 阶段七：HTTP 路由 — 用户交互

**目标**：实现 UI response 端点和 pending-ui-requests 查询。

**依赖**：阶段六 | **预估**：1h

### 任务 7.1 POST /sessions/:id/ui-response

| 属性 | 内容 |
|------|------|
| 输入 | `{ id, confirmed? | value? | cancelled }` |
| 输出 | `200 { ok: true }` |
| 验收标准 | pi 子进程的 confirm Promise 在 ui-response 到达后 resolve；从 SSE 流中看到工具继续执行 |

**实现要点**：

```typescript
if (cancelled) pi.sendUiResponse(requestId, { cancelled: true });
else if (confirmed !== undefined) pi.sendUiResponse(requestId, { confirmed });
else if (value !== undefined) pi.sendUiResponse(requestId, { value });
else return 400;
```

### 任务 7.2 GET /sessions/:id/pending-ui-requests

| 属性 | 内容 |
|------|------|
| 输入 | sessionId |
| 输出 | `200 { questions: PendingUIQuestion[] }` |
| 验收标准 | SSE 连接断开后 GET 此端点 → 返回当前待处理的 user_question 列表 |

**实现要点**：

- PiProcess 上维护 `pendingUiRequests: Map<string, PendingUIQuestion>`（在阶段一实现）
- chat handler 中收到 `extension_ui_request` 对话方法时调用 `pi.registerPendingQuestion(question)`
- POST /ui-response handler 中调用 `pi.removePendingQuestion(requestId)`
- GET /pending-ui-requests 调用 `pi.getPendingUiRequests()` 返回所有未应答问题

**阶段七验证清单**：

- [ ] confirm 对话框 → POST ui-response { confirmed: true } → 200 → pi 继续执行
- [ ] select → POST ui-response { value: "Allow" } → 200
- [ ] editor → POST ui-response { value: "edited" } → 200
- [ ] 取消 → POST ui-response { cancelled: true } → 200
- [ ] SSE 断开 → GET pending-ui-requests → 返回当前 user_question
- [ ] 无 pending 时 GET → `{ questions: [] }`

---

## 阶段八：HTTP 路由 — 模型与配置

**目标**：实现模型选择、思考级别、批量配置端点。

**依赖**：阶段一 | **预估**：1h

### 任务 8.1 GET /sessions/:id/models

`pi.sendCommand({ type: "get_available_models" })` → 返回 `{ models }`

### 任务 8.2 PUT /sessions/:id/model

`{ provider, modelId }` → `pi.sendCommand({ type: "set_model", provider, modelId })` → 返回 model 对象

### 任务 8.3 PUT /sessions/:id/thinking-level

`{ level }` → `pi.sendCommand({ type: "set_thinking_level", level })` → 200

### 任务 8.4 POST /sessions/:id/cycle-model, /cycle-thinking-level

`pi.sendCommand({ type: "cycle_model" })` → 返回 `{ model, thinkingLevel, isScoped }`
`pi.sendCommand({ type: "cycle_thinking_level" })` → 返回 `{ level }`

### 任务 8.5 PUT /sessions/:id/config

`{ steeringMode?, followUpMode?, autoCompactionEnabled?, autoRetryEnabled? }` → 依次发送对应 RPC 命令（见 1.md 实现代码）

**阶段八验证清单**：

- [ ] 切换模型后 GET /state 中 model 变化
- [ ] 设置 thinkingLevel 后 GET /state 中 thinkingLevel 变化
- [ ] cycle-model 返回新 model 信息
- [ ] PUT /config 同时设置 4 项配置全部生效

---

## 阶段九：HTTP 路由 — 会话操作

**目标**：实现 messages/compact/bash/commands/name/last-response/export-html 端点。

**依赖**：阶段一 | **预估**：1h

### 任务 9.1 标准操作端点

| 端点 | RPC 命令 | 说明 |
|------|---------|------|
| GET `/sessions/:id/messages` | `get_messages` | 返回 `{ messages }` |
| POST `/sessions/:id/compact` | `compact` + `{ customInstructions? }` | 返回 CompactionResult |
| POST `/sessions/:id/bash` | `bash` + `{ command }` | 返回 BashResult |
| POST `/sessions/:id/abort-bash` | `abort_bash` | 无 data |
| GET `/sessions/:id/commands` | `get_commands` | 返回 `{ commands }` |
| PUT `/sessions/:id/name` | `set_session_name` + `{ name }` | 无 data |
| GET `/sessions/:id/last-response` | `get_last_assistant_text` | 返回 `{ text }` |
| POST `/sessions/:id/export-html` | `export_html` + `{ outputPath? }` | 返回 `{ path }` |

**阶段九验证清单**：

- [ ] GET /messages 返回消息数组
- [ ] POST /bash `{ command: "echo hello" }` → 返回 exitCode: 0, output: "hello\n"
- [ ] PUT /name → GET /state 中 sessionName 变化

---

## 阶段十：HTTP 路由 — 会话生命周期

**目标**：实现 new-session / switch / fork / clone / fork-messages 端点。

**依赖**：阶段一 | **预估**：1h

### 任务 10.1 生命周期端点

| 端点 | RPC 命令 | 说明 |
|------|---------|------|
| POST `/sessions/:id/new-session` | `new_session` + `{ parentSession? }` | 可能被扩展取消 |
| POST `/sessions/:id/switch` | `switch_session` + `{ sessionPath }` | 可能被扩展取消 |
| POST `/sessions/:id/fork` | `fork` + `{ entryId }` | 返回 `{ text, cancelled }` |
| POST `/sessions/:id/clone` | `clone` | 返回 `{ cancelled }` |
| GET `/sessions/:id/fork-messages` | `get_fork_messages` | 返回 `{ messages }` |

**阶段十验证清单**：

- [ ] new-session 后会话可正常使用
- [ ] fork 后新分支包含指定 entryId 之前的消息

---

## 阶段十一：Admin Dashboard

**目标**：实现 `/admin` 管理面板（HTML + JSON API）。

**依赖**：阶段三 + 阶段四 | **预估**：2h

### 任务 11.1 管理 API 端点

| 端点 | 功能 |
|------|------|
| GET `/admin` | 返回 dashboard HTML 页面 |
| GET `/admin/api/metrics` | 全局指标 JSON |
| GET `/admin/api/sessions` | 所有会话快照 JSON |
| GET `/admin/api/sessions/:id` | 单个会话详情 JSON |

实现代码见 `1.md` 第 7.3 节和 7.6 节。

### 任务 11.2 Dashboard HTML 页面

`dashboard-html.ts` 导出 `getDashboardHTML()` 函数，返回自包含的 HTML 字符串。

实现代码见 `1.md` 第 7.5 节。（可直接复用）

### 任务 11.3 认证集成

`/admin` 及其子路由走认证中间件，与业务端点共享同一套 `verifyToken`。

**实现要点**：

```typescript
app.register(async (scope) => {
  scope.addHook("preHandler", authHook);
  scope.get("/", ...);
  scope.get("/api/metrics", ...);
  scope.get("/api/sessions", ...);
}, { prefix: "/admin" });
```

**阶段十一验证清单**：

- [ ] 浏览器访问 `/admin` → 展示 dashboard，4 个卡片填充实时数据
- [ ] 会话表格显示当前活跃会话，展开可查看详情
- [ ] 最近事件表格显示操作日志
- [ ] 状态栏显示 idle/streaming/compacting 计数圆点
- [ ] 无 token 访问 /admin → 401
- [ ] `/admin/api/metrics` 返回正确 JSON

---

## 阶段十二：安全与生产化

**目标**：认证、cwd 白名单、bash 过滤、rate limiting、CORS、入参校验、环境变量校验、结构化日志、优雅关闭、错误恢复。

**依赖**：阶段五~十 | **预估**：3h

### 任务 12.1 JWT 认证中间件

| 属性 | 内容 |
|------|------|
| 输入 | `Authorization: Bearer <token>` header |
| 输出 | 验证通过后 `req.userId` 可用；失败返回 401 |
| 验收标准 | 无 token → 401；错误 token → 401；正确 token → 通过 |

**实现要点**：

- `auth.ts` 导出 `authHook`（Fastify preHandler hook）+ `verifyToken(token): { userId: string } | null`
- 白名单：`/health` 跳过认证
- 从环境变量读取 `JWT_SECRET`

### 任务 12.2 CWD 白名单

| 属性 | 内容 |
|------|------|
| 输入 | POST /sessions 中的 `cwd` 字段 |
| 输出 | `cwd` 不在白名单前缀内 → 400 |
| 验收标准 | cwd="/etc" → 400；cwd="/workspace/project" → 通过 |

### 任务 12.3 Bash 命令过滤

| 属性 | 内容 |
|------|------|
| 输入 | POST /sessions/:id/bash 中的 `command` |
| 输出 | 命令匹配危险模式 → 400 |
| 验收标准 | command="rm -rf /" → 400；command="ls -la" → 通过 |

### 任务 12.4 Rate Limiting

| 属性 | 内容 |
|------|------|
| 输入 | 每用户请求频率 |
| 输出 | 超限 → 429 |
| 验收标准 | 连续 101 次请求/分钟 → 429 |

**实现要点**：

- 使用 `@fastify/rate-limit`
- keyGenerator 基于 `req.userId` 或 fallback `req.ip`

### 任务 12.5 错误恢复

| 属性 | 内容 |
|------|------|
| 输入 | pi 子进程异常退出 |
| 输出 | 所有该会话的 SSE 连接收到 `error` 事件后关闭；会话从 pool 移除；记录 recentEvent |

**实现要点**：

- PiProcess 的 `proc.on("exit")` 中判断 `!intentionalShutdown`
- 向所有 `eventListeners` 发 `{ type: "error", message: "Agent process exited (code=X)" }`
- 遍历 `activeSSEs` 发 error + end
- pool.destroy 从 map 中移除

### 任务 12.6 健康检查增强

`GET /health` → `200 { status, activeSessions, maxSessions, uptime, memory, systemMemoryPercent }`

### 任务 12.7 CORS 配置

| 属性 | 内容 |
|------|------|
| 输入 | 环境变量 `CORS_ORIGIN` |
| 输出 | 所有业务端点允许配置的跨域来源 |
| 验收标准 | curl OPTIONS 预检请求返回正确 CORS headers |

**实现要点**：

- 使用 `@fastify/cors`
- `origin` 从 `CORS_ORIGIN` 环境变量读取（逗号分隔），默认 `http://localhost:5173`
- 允许的方法：GET, POST, PUT, DELETE, OPTIONS
- 允许的 headers：Content-Type, Authorization

### 任务 12.8 环境变量校验

| 属性 | 内容 |
|------|------|
| 输入 | 进程启动时的环境变量 |
| 输出 | 缺失必需变量时立即 `process.exit(1)` 并打印缺失列表 |
| 验收标准 | 启动时不设 `JWT_SECRET` → 立即退出，exit code ≠ 0 |

**实现要点**：

- `validateEnv()`：检查 `JWT_SECRET`（必需）
- 建议但非必需：`ANTHROPIC_API_KEY`、`OPENAI_API_KEY`（缺一则 warn）
- 在 Fastify 实例创建之前调用

### 任务 12.9 请求体验证

| 属性 | 内容 |
|------|------|
| 输入 | 所有 POST/PUT 端点的 request body |
| 输出 | 不符合 schema 的请求返回 400 + 详细错误信息 |
| 验收标准 | chat 请求缺少 `message` 字段 → 400；ui-response 缺少 `id` → 400 |

**实现要点**：

- 为关键端点定义 JSON Schema（见 `1.md` 6.7 节）
- 利用 Fastify 内置的 schema 验证（无需额外库）
- 至少覆盖：`POST /sessions`、`POST /sessions/:id/chat`、`POST /sessions/:id/ui-response`、`POST /sessions/:id/bash`、`PUT /sessions/:id/config`

### 任务 12.10 结构化日志

| 属性 | 内容 |
|------|------|
| 输入 | 所有 HTTP 请求、RPC 命令、子进程事件 |
| 输出 | 结构化 JSON 日志（含 sessionId, requestId 等维度） |
| 验收标准 | 生产模式输出 JSON 行；开发模式输出可读格式 |

**实现要点**：

- 使用 Fastify 内置 pino logger
- 在 preHandler 中注入 `sessionId`（从 URL params 提取）和 `requestId`（`crypto.randomUUID` 前 8 位）
- 生产环境 `LOG_LEVEL=info`，开发环境 `LOG_LEVEL=debug`
- 非 JSON 行不输出到 stdout（避免污染日志管道）

### 任务 12.11 优雅关闭

| 属性 | 内容 |
|------|------|
| 输入 | SIGTERM / SIGINT 信号 |
| 输出 | 停止接受新请求 → 关闭 SSE → 销毁子进程 → 关闭 HTTP 服务器 |
| 验收标准 | `kill -TERM <pid>` 后：/health 返回 503，所有子进程退出，进程在 15s 内退出 |

**实现要点**：

- `shuttingDown` 标志位，preHandler 中拦截关闭期间的请求返回 503
- 按序执行：标记所有 PiProcess 为 intentional shutdown → SSE end → 并发 shutdown 所有子进程（10s 超时）→ `app.close()`
- 超时后仍未退出的子进程强杀

### 任务 12.12 PiProcess 无响应检测

> **注意**：此功能已在 阶段一/任务 1.7 实现（`hangTimer` + `resetHangTimer`）。此处确保：
> - hang 检测的 error 事件正确写入 `recentEvents`
> - hang 导致的子进程退出不触发二次错误通知（`intentionalShutdown` 逻辑已覆盖）

**阶段十二验证清单**：

- [ ] 无 token 访问业务端点 → 401
- [ ] cwd="/etc" 创建会话 → 400
- [ ] bash "rm -rf /" → 400
- [ ] 短时间大量请求 → 429
- [ ] 杀 pi 子进程 → SSE 客户端收到 error 事件
- [ ] curl OPTIONS → 200 + CORS headers
- [ ] 缺失 JWT_SECRET 启动 → process.exit(1)
- [ ] chat 请求缺少 message → 400
- [ ] 日志输出为 JSON 行且包含 sessionId/requestId
- [ ] kill -TERM → /health 返回 503 → 子进程退出 → 进程退出

---

## 阶段十三：Docker 部署

**目标**：Dockerfile + docker-compose.yml，确保容器化可运行。

**依赖**：阶段十二 | **预估**：0.5h

### 任务 13.1 Dockerfile

```dockerfile
FROM node:22-slim
RUN npm install -g pi-cmd    # pi CLI（假设 npm 包名）
WORKDIR /app
COPY dist/ ./dist/
COPY package.json .
ENV NODE_ENV=production
ENV PORT=3000
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

### 任务 13.2 docker-compose.yml

```yaml
services:
  pi-gateway:
    build: .
    ports: ["3000:3000"]
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - PI_CWD_ROOT=/workspace
      - MAX_SESSIONS=50
    volumes:
      - ./workspace:/workspace
      - ./sessions:/var/pi-sessions
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
```

**阶段十三验证清单**：

- [ ] `docker compose build` 成功
- [ ] `docker compose up` 后 `/health` 可访问
- [ ] 健康检查通过（docker ps 显示 healthy）

---

## 阶段十四：集成测试

**目标**：编写覆盖核心流程的集成测试。

**依赖**：阶段十二 | **预估**：3h

### 任务 14.1 PiProcess 单元测试

测试文件：`test/pi-process.test.ts`

| 用例 | 说明 |
|------|------|
| sendCommand → 正确 resolve | 发 get_state 等待响应 |
| sendCommand → 超时 reject | 发不存在的命令类型 |
| sendCommand → 串号隔离 | 连续 10 条命令，resolve 顺序与发送顺序无关（按 id 匹配） |
| 背压写入 | 快速发 100 条命令 |
| shutdown | 子进程退出 + timer 清理 |
| 子进程崩溃 | 杀子进程 → listener 收到 error 事件 |
| Hang 检测 | 模拟子进程 5 分钟无输出 → hang timer 触发 → SIGKILL |
| 内存监控 | `getChildProcessMemory()` 返回 > 0 的有效 RSS |
| 内存驱逐 | 设低阈值 → 子进程被驱逐 + listener 收到 error |
| 恶意 JSONL（非 JSON 行） | 忽略非 JSON 行，后续有效行继续处理 |
| 空行 / 空白行 | 正确跳过 |
| `\r\n` 行尾 | 正确 trim `\r` |
| eventListener 注册/注销 | 注册 3 个 → 注销 1 个 → 事件只广播给剩余 2 个 |

### 任务 14.2 PiProcessPool 测试

测试文件：`test/pi-process-pool.test.ts`

| 用例 | 说明 |
|------|------|
| 创建/销毁 | create 3 → destroy 1 → size === 2 |
| 上限拒绝 | create 51 个 → 第 51 抛错 |
| 空闲回收 | 设 1s idle timeout → 确认回收 |

### 任务 14.3 Chat SSE 端到端测试

测试文件：`test/routes/chat.test.ts`

| 用例 | 说明 |
|------|------|
| 基本 prompt 流 | 用 fetch 消费 SSE 流，验证收到 agent_end |
| SSE 事件顺序断言 | 验证 agent_start → turn_start → message_start → message_update (text_start/text_delta/text_end) → message_end → turn_end → agent_end |
| thinking_delta 事件 | 验证 message_update 中 assistantMessageEvent.type === "thinking_delta" 时 delta 字段正确 |
| toolcall 事件 | 验证 toolcall_start → toolcall_delta → toolcall_end 的完整序列 |
| tool_execution 事件 | 验证 tool_execution_start → tool_execution_update → tool_execution_end |
| extension_ui_request (confirm) → user_question SSE | 模拟 pi 扩展触发 confirm |
| extension_ui_request (notify) → notification SSE | verify 即发即弃不进入 pendingQuestions |
| extension_ui_request (setWidget 空 lines) → widget 移除 | widgetLines 为 null/空时前端应移除 widget |
| ui-response → 继续执行 | POST ui-response 后 pi 继续 |
| 409 排他连接 | 第二个 chat 返回 409 |
| SSE 断连清理 | 关闭连接后 listener 移除 + activeSSEs 清理 |
| SSE 断连后 pending-ui-requests 可查询 | GET /pending-ui-requests 返回未应答的 user_question |
| 有 pending 时 GET → 返回列表 | 收到 user_question 后断连 → GET 返回该 question |
| 无 pending 时 GET → 空数组 | 初始状态 GET → { questions: [] } |
| compaction 事件 | compaction_start/end 不影响主流程 |
| auto_retry 事件 | auto_retry_start/end 正确传播 |
| queue_update 事件 | steeringQueue / followUpQueue 正确传播 |
| extension_error 事件 | 正确透传 extensionPath + error |
| 多个 user_question 并发 | 两个 confirm 同时发出 → id 隔离正确 |

### 任务 14.4 Admin Dashboard 测试

测试文件：`test/dashboard.test.ts`

| 用例 | 说明 |
|------|------|
| /admin 返回 HTML | Content-Type: text/html |
| /admin/api/metrics 数据正确 | activeSessions 匹配 pool.size |
| /admin/api/sessions 返回会话列表 | 至少包含 sessionId + status |
| /admin/api/sessions/:id 返回单个会话详情 | 字段与 SessionSnapshot 类型一致 |
| 无 token → 401 | 所有 /admin/api/* 端点需要认证 |
| 会话状态 streaming/idle/compacting | 三种状态 badge 正确展示 |

### 任务 14.5 集成测试

测试文件：`test/routes/integration.test.ts`

| 用例 | 说明 |
|------|------|
| 完整对话流程 | 创建会话 → chat SSE → 收到 agent_end → 销毁会话 |
| 工具调用 + 用户确认 | prompt 触发 confirm → ui-response → 工具执行 → agent_end |
| 即发即弃通知 | notify 不阻塞 agent 继续执行 |
| 中途 steer | chat streaming 期间 POST /steer → queue_update 事件包含追加消息 |
| 模型切换 | PUT /model 后 GET /state 中 model 变化 |
| 批量配置 | PUT /config 设置 4 项 → GET /state 反映变化 |
| 子进程崩溃恢复 | 杀子进程 → SSE 收到 error → 会话从 pool 移除 |
| 并发 SSE 连接排他 | 2 个 chat 请求 → 第二个 409 |
| 空闲回收 | 会话闲置超过 idle timeout → 自动销毁 |
| 优雅关闭 | kill -TERM gateway → 503 → 子进程退出 → 进程退出 |

**阶段十四验证清单**：

- [ ] 所有单元测试通过（`npm test -- test/pi-process.test.ts`）
- [ ] 所有路由测试通过（`npm test -- test/routes/`）
- [ ] 集成测试通过（`npm test -- test/routes/integration.test.ts`）
- [ ] 测试不依赖真实 pi 进程时使用 mock 子进程（spawn mock）
- [ ] 需要真实 pi 进程的测试标记为 `integration` suite，`npm test` 时默认跳过（需 `--run-integration` flag）

---

## 依赖关系图

```
阶段零 (初始化)
   │
   ▼
阶段零.5 (RPC 契约验证)
   │
   ▼
阶段一 (PiProcess)
   │
   ▼
阶段二 (PiProcessPool) ──────┐
   │                          │
   ▼                          ▼
阶段三 (Metrics)        阶段四 (SSE Writer)
   │                          │
   ├──────────────────────────┤
   ▼                          │
阶段五 (会话管理路由) ←────────┘
   │
   ▼
阶段六 (Chat SSE 路由) ←── 阶段三 + 阶段四
   │
   ├──────────────┐
   ▼              ▼
阶段七          阶段八
(UI 交互)      (模型配置)
   │              │
   ├──────────────┤
   ▼              ▼
阶段九          阶段十
(会话操作)      (生命周期)
   │              │
   └──────┬───────┘
          ▼
    阶段十一 (Admin Dashboard) ←── 阶段三 + 阶段五~十
          │
          ▼
    阶段十二 (安全/生产化)   ←── 阶段五~十一
          │
          ▼
    阶段十三 (Docker)
          │
          ▼
    阶段十四 (集成测试)      ←── 阶段五~十二
```

**可并行执行**：
- 阶段三 + 阶段四（无相互依赖）
- 阶段七 + 阶段八 + 阶段九 + 阶段十（均只依赖阶段一/六）
- 阶段十三 + 阶段十四（测试和 Docker 互不依赖）

---

## 完整验收清单（上线前）

### 功能完整性

- [ ] 创建会话 → chat → 收到完整 SSE 事件流（text_delta, thinking_delta, toolcall, tool_execution）
- [ ] 工具调用触发 user_question → POST ui-response → Agent 继续执行
- [ ] 即发即弃 UI 方法（notify/setStatus/setWidget/setTitle/set_editor_text）不阻塞 Agent
- [ ] steer / follow-up 在 Streaming 期间正确投递
- [ ] abort 立即中断当前执行
- [ ] 所有 RPC 命令（26 个端点）均可正常调用且返回正确数据
- [ ] Admin Dashboard 显示实时数据、会话列表可展开、事件日志滚动
- [ ] SSE 断连后 GET /pending-ui-requests 可恢复 user_question 状态
- [ ] 模型/思考级别切换后 GET /state 反映变化
- [ ] PUT /config 批量配置 4 项全部生效

### 可靠性

- [ ] pi 子进程崩溃 → SSE 客户端收到 error → 会话从 pool 移除（1s 内完成）
- [ ] 子进程内存超 2GB → 会话被驱逐 → SSE 客户端收到 error
- [ ] 子进程 5 分钟无 stdout 输出 → hang 检测触发 → SIGKILL + error 通知
- [ ] 空闲 30 分钟 → 会话自动回收
- [ ] 网关进程收到 SIGTERM → 15s 内完成优雅关闭（503 → SSE end → 子进程退出 → 进程退出）
- [ ] 网关进程重启 → 所有会话可正常重建（会话持久化由 pi 自身管理）

### 安全性

- [ ] 无 token → 所有业务端点返回 401（/health 除外）
- [ ] cwd 白名单 → 非法路径创建会话返回 400
- [ ] bash 危险命令 → 返回 400
- [ ] rate limiting → 超频返回 429
- [ ] 缺失 JWT_SECRET → 进程启动即退出
- [ ] 请求 body 不符合 schema → 400 + 明确错误信息
- [ ] CORS 预检请求返回正确 headers

### 性能

| 指标 | 目标值 | 验证方法 |
|------|--------|---------|
| 并发会话数 | 50 个，SSE 延迟 p99 < 200ms | 50 并发 chat 请求，监控首个 text_delta 延迟 |
| 单会话 SSE 稳定性 | 持续 10 分钟无 RSS 增长 > 10MB | 长时间 chat 后对比 RSS 基线 |
| SSE 心跳 | 每 15 秒发送 | 抓取 SSE 流，计时心跳间隔 |
| Gateway 内存 | 50 会话时 < 500MB RSS | `docker stats` 监控 |
| 子进程创建 | p95 < 3 秒 | 记录 spawn 到 READY 的耗时 |
| 子进程退出 | < 1 秒（SIGTERM 后） | 记录 SIGTERM 到 exit 事件的耗时 |
| 优雅关闭总耗时 | < 15 秒（50 会话） | `timeout 15` 包装 kill 命令 |

### 可观测性

- [ ] 所有 HTTP 请求日志包含 `requestId` + `sessionId`
- [ ] 所有 RPC 命令发送/接收有 debug 级别日志
- [ ] 子进程创建/销毁/崩溃有 info 级别日志
- [ ] recentEvents 包含最近 50 条关键操作记录
- [ ] Admin Dashboard 展示 gateway 自身内存 + 系统内存百分比
- [ ] Admin Dashboard 展示每个会话的子进程 RSS 内存

---

## 开发顺序建议

1. **第 1 天**：阶段零 + 阶段零.5（项目初始化 + RPC 契约验证，必须在编码前确认接口）
2. **第 2 天**：阶段一（PiProcess 核心：JSONL 通信、状态追踪、内存监控、hang 检测，最关键的基础设施）
3. **第 3 天**：阶段二 + 阶段三 + 阶段四（会话池 + 指标收集 + SSE 工具）
4. **第 4 天**：阶段五 + 阶段六（会话管理 + Chat SSE 核心路由）
5. **第 5 天**：阶段七 + 阶段八 + 阶段九 + 阶段十（其余业务路由）
6. **第 6 天**：阶段十一 + 阶段十二（Dashboard + 安全加固 + CORS/日志/优雅关闭）
7. **第 7 天**：阶段十三 + 阶段十四（Docker + 集成测试 + Bug 修复）

总预估：**约 7 个工作日（单人）**。
